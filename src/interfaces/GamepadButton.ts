export enum GamepadButton {
  // Main buttons
  A = 'A',
  B = 'B',
  X = 'X',
  Y = 'Y',

  // Shoulders (controller front top buttons)
  LS = 'LS',
  RS = 'RS',

  // Triggers (controller front bottom buttons)
  LT = 'LT',
  RT = 'RT',

  // Control buttons
  Select = 'Select',
  Start = 'Start',

  // Pressable buttons at axes
  LAB = 'LAB',
  RAB = 'RAB',

  // Left Axis (if behaves as buttons)
  LAT = 'LAT',
  LAD = 'LAD',
  LAL = 'LAL',
  LAR = 'LAR',

  // Right Axis (if behaves as buttons)
  RAT = 'RAT',
  RAD = 'RAD',
  RAL = 'RAL',
  RAR = 'RAR',

  // Pad (cross)
  Up = 'Up',
  Down = 'Down',
  Left = 'Left',
  Right = 'Right',
}
